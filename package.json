{
  "scripts": {
    "fix": "standard --fix",
    "prepare": "husky install"
  },
  "standard": {
    "globals": [
      "fetch",
      "browser",
      "MutationObserver"
    ]
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "lint-staged": "^13.0.0",
    "standard": "^17.0.0"
  },
  "lint-staged": {
    "*.js": "standard --fix"
  }
}
